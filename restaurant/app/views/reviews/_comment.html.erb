<div class='container contentWrap content'>
<%= form_for([@resturant, @review]) do |form| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@review.errors.count, "error") %> prohibited
        this review from being saved:
      </h2>
      <ul>
        <% @review.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <div>
    <%= form.label :title %>
    </div>
    <%= form.text_field :title %>
  </div>
  <br>
  <div class="field">
    <div>
    <%= form.label :body %>
    </div>
    <%= form.text_area :body %>
  </div>
<br><br>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
<br>
<%= link_to 'Back', resturant_path(@resturant) %>